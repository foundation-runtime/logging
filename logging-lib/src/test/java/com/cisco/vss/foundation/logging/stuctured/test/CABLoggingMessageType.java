package com.cisco.vss.foundation.logging.stuctured.test;

public enum CABLoggingMessageType {
    STATE_MESSAGE, // state message (start or stop)
    ERROR_MESSAGE, // error message
    TRANS_MESSAGE // transactional message
}
